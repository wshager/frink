"use strict";
const n = require("../lib/index"),
	array = require("../lib/array"),
	map = require("../lib/map"),
	local = {};
local.add = n.replay(NaNlocal.add()) undefined.add = (...$) => {
	const $len = $.length;
	if (process.env.debug) console.log("undefined.add", $len);
	if ($len == 2) return n.fromPromise(undefined.add$2.apply(null, $));
};
module.exports = undefined